version: '3.4'

services:
  rabbitmq:
    image: rabbitmq:3-management
    container_name: rabbitmq
    restart: on-failure
    ports:
      - 15672:15672
      - 5672:5672
    volumes:
      - ./volumes/rabbitmq_volume:/var/lib/rabbitmq/mnesia

  redis:
    image: redis/redis-stack-server:latest
    container_name: redis-stack-server
    restart: on-failure
    command: redis-server --appendonly yes
    ports:
      - 6379:6379 
    volumes:
      - ./volumes/redis_volume:/data
  mssql:
    image: mcr.microsoft.com/mssql/server:2022-latest
    ports:
      - 5000:1433
    volumes:
      - ./volumes/data:/var/opt/mssql/data
      - ./volumes/log:/var/opt/mssql/log
      - ./volumes/secrets:/var/opt/mssql/secrets
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=rita0504!@#
  ui:
    build:
      context: .
      dockerfile: ShopApp.WebPL/Dockerfile
    ports:
      - 5001:80
